---
description: Run the Vitest unit test suite.
agent: build
---

Run `pnpm test:run`. If any tests fail, diagnose the cause (either fix the test or the underlying code, following the repo's `test/` by-type layout), then re-run until green. Report pass/fail summary and what was fixed.
