---
description: Run the production build.
agent: build
---

Run `pnpm build`. Fix any build errors (type errors, lint failures, static analysis, route conflicts) and rebuild until it succeeds. Report the final build result.
